<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_graph"
    app:startDestination="@id/profileGraph">

    <include app:graph="@navigation/profile_graph" />
    <include app:graph="@navigation/meter_creator_graph" />
    <include
        android:id="@+id/loginFlow"
        app:graph="@navigation/login_graph" />
    <fragment
        android:id="@+id/loginFragment"
        android:name="com.rodionov.meters_data.presentation.login.LoginFragment"
        android:label="LoginFragment"
        tools:layout="@layout/fragment_login"/>

</navigation>